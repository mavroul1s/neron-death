% Architecture + where the instrument reads. PAPER_BRIEF §8.2 asks for this to be
% schematic rather than a literal layer-by-layer box diagram: the point is not
% the MLP (which is unremarkable) but *where the two probe batches enter* and
% *when recycling fires relative to task boundaries*.
%
% Laid out to fit TMLR's 6.5in text width; see the geometry note in fig_arms.tex.
\begin{figure}[t]
\centering
\begin{tikzpicture}[
  font=\small,
  layer/.style = {draw=black!45, rounded corners=1pt, minimum width=11mm,
                  minimum height=13mm, align=center, font=\scriptsize, fill=white},
  probe/.style = {draw=#1, rounded corners=1.5pt, fill=#1!8, align=center,
                  font=\scriptsize, inner sep=2.6pt},
  sub/.style   = {font=\scriptsize, text=black!60},
  note/.style  = {font=\scriptsize, text=black!60, text width=46mm, align=left},
  ar/.style    = {->, black!55, line width=0.55pt, shorten >=1.5pt, shorten <=1.5pt},
]
\definecolor{cCur}{HTML}{2A78D6}
\definecolor{cRef}{HTML}{EB6834}
\definecolor{cEv}{HTML}{1BAF7A}

% ---- the network -------------------------------------------------------------
\node[layer] (in)  at (0,0)   {$784$\\input};
\node[layer] (h1)  at (1.7,0) {$500$\\ReLU};
\node[layer] (h2)  at (3.4,0) {$500$\\ReLU};
\node[layer] (h3)  at (5.1,0) {$500$\\ReLU};
\node[layer] (out) at (6.8,0) {$10$\\logits};
\foreach \a/\b in {in/h1, h1/h2, h2/h3, h3/out} \draw[ar] (\a) -- (\b);

\draw[decorate, decoration={brace, amplitude=3pt}, black!45]
  (h1.north west) ++(0,0.14) -- ++(4.5,0)
  node[midway, above, sub, yshift=0.6mm]
  {hidden layers $\ell \in \{0,1,2\}$ --- the units this paper counts};

% ---- the two probe batches ---------------------------------------------------
\node[probe=cCur, anchor=east] (pc) at (-1.05,0.60)
  {\textbf{current-task probe}\\$2048$ images, permutation $\pi_t$};
\node[probe=cRef, anchor=east] (pr) at (-1.05,-0.62)
  {\textbf{reference probe}\\$2048$ images, \emph{fixed} all run};
\draw[ar, cCur] (pc.east) -- (in.north west);
\draw[ar, cRef] (pr.east) -- (in.south west);

% ---- the training timeline ---------------------------------------------------
\def\ty{-2.35}
\draw[black!45, line width=0.6pt] (-4.7,\ty) -- (7.1,\ty);
\foreach \x/\lab in {-4.4/{task $t$}, -0.6/{task $t{+}1$}, 3.2/{task $t{+}2$}}{
  \draw[black!45, line width=0.9pt] (\x,\ty-0.14) -- (\x,\ty+0.28);
  \node[sub, anchor=south west] at (\x+0.06,\ty+0.28) {\lab};
}
\draw[black!45, line width=0.9pt] (7.0,\ty-0.14) -- (7.0,\ty+0.28);

\foreach \x in {-4.4, -0.6, 3.2, 7.0}{
  \draw[cCur, line width=0.7pt] (\x,\ty+0.28) -- (\x,\ty+0.55);
  \node[circle, fill=cCur, inner sep=1.1pt] at (\x,\ty+0.55) {};
}
\foreach \x in {-2.2, 1.6, 5.4}{
  \draw[cEv, line width=0.7pt, densely dashed] (\x,\ty-0.14) -- (\x,\ty-0.42);
  \node[circle, fill=cEv, inner sep=1.1pt] at (\x,\ty-0.42) {};
}
\node[sub, anchor=west] at (-4.7,\ty+0.85)
  {\textcolor{cCur}{$\bullet$}~full probe at every task boundary: every metric,
   on both batches, per neuron};
\node[sub, anchor=west] at (-4.7,\ty-0.78)
  {\textcolor{cEv}{$\bullet$}~recycling event: score every unit, select $k$,
   reset --- fires every $F{=}1000$ optimizer steps against $469$ steps per
   task,};
\node[sub, anchor=west] at (-4.7,\ty-1.06)
  {\phantom{\textcolor{cEv}{$\bullet$}~}so events and task boundaries are
   deliberately unaligned};

\end{tikzpicture}
\caption{\textbf{Setting and instrument.} A $784$--$500$--$500$--$500$--$10$
ReLU MLP on online permuted MNIST: each task is a fresh input permutation seen
for a single pass, and the outcome measure is online accuracy, scored on the
forward pass taken \emph{before} each update. Every death metric is evaluated on
two probe batches --- the current task's distribution and a reference
distribution fixed for the entire run --- and both are logged, because the gap
between them is what separates a unit that is dead from one that is merely
silent on the permutation being trained on right now. Per-neuron state is
written at every task boundary; recycling events are governed by an
optimizer-step counter rather than by task index, so they fall inside tasks
rather than at their edges.}
\label{fig:arch}
\end{figure}
