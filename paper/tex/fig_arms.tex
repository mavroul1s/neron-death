% The three recycling arms, side by side. PAPER_BRIEF §8.1: "the single most
% important explanatory diagram in the paper" -- a reader who follows this
% follows the whole experimental logic.
%
% What it has to make unmissable: k is not a design constant. It is read off the
% tau-dormant set at each event, in each layer, and then all three arms recycle
% exactly that many units. What differs across arms is only *which* k.
\begin{figure}[t]
\centering
\begin{tikzpicture}[
  font=\small,
  unit/.style   = {circle, draw=black!45, minimum size=4.6mm, inner sep=0pt, line width=0.4pt},
  dead/.style   = {unit, fill=black!62, draw=black!62},
  quiet/.style  = {unit, fill=black!22, draw=black!45},
  alive/.style  = {unit, fill=white},
  pick/.style   = {draw=#1, line width=1.1pt},
  lbl/.style    = {font=\footnotesize\bfseries},
  sub/.style    = {font=\scriptsize, text=black!62},
]

\definecolor{cRedo}{HTML}{2A78D6}
\definecolor{cRand}{HTML}{EB6834}
\definecolor{cInv}{HTML}{1BAF7A}

% ---- the score axis, shared by all three panels -------------------------------
\def\ux{0.62}
\node[sub, anchor=west] at (-0.35,1.62) {a layer at one recycling event, units ordered by Sokar score $s_i$};
\foreach \i in {0,...,11}{
  \pgfmathsetmacro\xx{\i*\ux}
  \ifnum\i<3 \node[dead]  (u\i) at (\xx,1.0) {}; \else
  \ifnum\i<5 \node[quiet] (u\i) at (\xx,1.0) {}; \else
             \node[alive] (u\i) at (\xx,1.0) {}; \fi\fi
}
\draw[->, black!45, line width=0.5pt] (-0.36,0.62) -- (11*\ux+0.36,0.62)
  node[midway, below, sub, yshift=-0.5mm] {$s_i$ low $\longrightarrow$ high};
\draw[decorate, decoration={brace, amplitude=3pt, mirror}, black!55]
  (-0.30,1.30) -- (4*\ux+0.30,1.30)
  node[midway, above, sub, yshift=0.3mm] {$\tau$-dormant set, $|{\cdot}| = k$};

% ---- legend ------------------------------------------------------------------
\node[dead]  (lg1) at (12.4*\ux+0.9,1.30) {}; \node[sub, right=1mm of lg1] {exactly zero};
\node[quiet] (lg2) at (12.4*\ux+0.9,0.86) {}; \node[sub, right=1mm of lg2] {quiet, but firing};
\node[alive] (lg3) at (12.4*\ux+0.9,0.42) {}; \node[sub, right=1mm of lg3] {ordinary};

% ---- the three arms ----------------------------------------------------------
% Each row: k = 5 units recycled, drawn from a different part of the ordering.
\def\rowy{-0.55}
\foreach \arm/\name/\col/\sel/\note in {%
  0/ReDo/cRedo/{0,1,2,3,4}/{the $k$ lowest-scoring: the $\tau$-dormant set itself},
  1/size-matched random/cRand/{1,4,6,8,11}/{$k$ units drawn uniformly from the layer},
  2/inverse-matched/cInv/{7,8,9,10,11}/{the $k$ \emph{highest}-scoring units}%
}{
  \pgfmathsetmacro\yy{\rowy - \arm*1.15}
  \node[lbl, anchor=east, text=\col] at (-0.55,\yy) {\name};
  \foreach \i in {0,...,11}{
    \pgfmathsetmacro\xx{\i*\ux}
    \ifnum\i<3 \node[dead]  (v\arm\i) at (\xx,\yy) {}; \else
    \ifnum\i<5 \node[quiet] (v\arm\i) at (\xx,\yy) {}; \else
               \node[alive] (v\arm\i) at (\xx,\yy) {}; \fi\fi
  }
  \foreach \s in \sel {\node[unit, pick=\col] at (\s*\ux,\yy) {};}
  \node[sub, anchor=west] at (12*\ux+0.28,\yy) {\note};
}

\draw[black!25, line width=0.4pt] (-2.7,0.18) -- (13.6,0.18);

\end{tikzpicture}
\caption{\textbf{The three recycling arms.} At every event, in every layer, the
$\tau$-dormancy score is computed and $k$ is set to the size of the
$\tau$-dormant set. All three arms then recycle exactly $k$ units by the
identical procedure --- re-initialise the incoming weights from the layer's
original initialisation distribution, zero the outgoing weights
\citep{sokar2023dormant} --- and differ only in \emph{which} $k$. Because $k$ is
recomputed per layer per event rather than fixed as a fraction or a schedule,
the comparison isolates the effect of targeting from the effect of dose. Circles
are units; shading is their state on the probe batch, which is measured
independently of the score that selects them.}
\label{fig:arms}
\end{figure}
